# Địa chỉ FastAPI, CHỈ đọc phía server (BFF proxy) — không phải NEXT_PUBLIC_*,
# nên đổi giá trị chỉ cần khởi động lại, không phải build lại image.
# Chạy bằng pnpm trên máy:   http://localhost:8000
# Chạy trong Docker:          http://host.docker.internal:8000
API_BASE_URL=http://localhost:8000
